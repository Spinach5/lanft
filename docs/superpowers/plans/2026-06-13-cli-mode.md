# CLI Mode — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add CLI mode — when args passed, run in terminal with progress bars; no args = GUI unchanged.

**Architecture:** `main.c` checks argc, routes to `cli_main()` in new `cli.c` for CLI path. `transfer.c` gets callback mechanism replacing hardcoded SDL events. CLI runs transfer synchronously in main thread.

**Tech Stack:** C11, getopt_long, existing libwebsockets/libarchive network modules

---

### Task 1: Add callback mechanism to transfer.c/h

**Files:**
- Modify: `src/transfer.h`
- Modify: `src/transfer.c`

- [ ] **Step 1: Add callback types and setter to transfer.h**

After the existing `#include <stdint.h>` and struct forward declarations, add:

```c
/* Progress callbacks — set by GUI or CLI before transfer */
typedef void (*transfer_progress_fn)(uint64_t done, uint64_t total);
typedef void (*transfer_error_fn)(const char *msg);
typedef void (*transfer_done_fn)(void);

void transfer_set_callbacks(transfer_progress_fn prog,
                            transfer_error_fn err,
                            transfer_done_fn done);
```

- [ ] **Step 2: Add callback storage and replace SDL event calls in transfer.c**

At the top of transfer.c, after the includes, add:

```c
/* ── Callback storage ──────────────────────────────────────── */
static transfer_progress_fn g_prog_cb = NULL;
static transfer_error_fn    g_err_cb  = NULL;
static transfer_done_fn     g_done_cb = NULL;

void transfer_set_callbacks(transfer_progress_fn prog,
                            transfer_error_fn err,
                            transfer_done_fn done)
{
    g_prog_cb = prog;
    g_err_cb  = err;
    g_done_cb = done;
}
```

- [ ] **Step 3: Replace push_progress calls with callback**

Find the `push_progress` function and replace its body. Change:

```c
static void push_progress(uint64_t done, uint64_t total)
{
    struct event_progress *p = calloc(1, sizeof(*p));
    if (!p) return;
    p->bytes_done = done;
    p->bytes_total = total;
    push_event(SDL_USEREVENT + 3, p);
}
```

To:

```c
static void push_progress(uint64_t done, uint64_t total)
{
    if (g_prog_cb) { g_prog_cb(done, total); return; }
    struct event_progress *p = calloc(1, sizeof(*p));
    if (!p) return;
    p->bytes_done = done;
    p->bytes_total = total;
    push_event(SDL_USEREVENT + 3, p);
}
```

- [ ] **Step 4: Replace push_error calls with callback**

Change:

```c
static void push_error(const char *fmt, ...)
{
    struct event_error *err = calloc(1, sizeof(*err));
    if (!err) return;
    va_list ap;
    va_start(ap, fmt);
    vsnprintf(err->message, sizeof(err->message), fmt, ap);
    va_end(ap);
    push_event(SDL_USEREVENT + 5, err);
}
```

To:

```c
static void push_error(const char *fmt, ...)
{
    char buf[512];
    va_list ap;
    va_start(ap, fmt);
    vsnprintf(buf, sizeof(buf), fmt, ap);
    va_end(ap);
    if (g_err_cb) { g_err_cb(buf); return; }
    struct event_error *err = calloc(1, sizeof(*err));
    if (!err) return;
    strncpy(err->message, buf, sizeof(err->message) - 1);
    push_event(SDL_USEREVENT + 5, err);
}
```

- [ ] **Step 5: Replace push_xfer_done with callback**

Find the `push_xfer_done` function and change:

```c
static void push_xfer_done(void)
{
    push_event(SDL_USEREVENT + 4, NULL);
}
```

To:

```c
static void push_xfer_done(void)
{
    if (g_done_cb) { g_done_cb(); return; }
    push_event(SDL_USEREVENT + 4, NULL);
}
```

- [ ] **Step 6: Build to verify no regressions**

```bash
cd /home/zqw/biancheng/Project/websocket/build && cmake .. && make -j$(nproc) 2>&1
```

Expected: Clean build.

- [ ] **Step 7: Commit**

```bash
cd /home/zqw/biancheng/Project/websocket && git add src/transfer.h src/transfer.c && git commit -m "feat: add transfer callbacks to decouple from SDL events"
```

---

### Task 2: Write cli.c — CLI argument parsing and transfer orchestration

**Files:**
- Create: `src/cli.c`

- [ ] **Step 1: Write cli.c**

```c
#define _GNU_SOURCE
#include "protocol.h"
#include "network.h"
#include "transfer.h"

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <getopt.h>
#include <time.h>
#include <sys/time.h>
#include <sys/stat.h>
#include <unistd.h>

#define CLI_VERSION "lanft v1.0"

/* ── Config ────────────────────────────────────────────────── */

struct cli_config {
    int  protocol;
    int  mode;         /* 0=S, 1=R */
    int  port;
    char address[64];
    char path[1024];
    bool show_history;
};

/* ── Progress bar ──────────────────────────────────────────── */

static uint64_t cli_start_ms;
static uint64_t cli_total;

static uint64_t now_ms(void) {
    struct timeval tv; gettimeofday(&tv, NULL);
    return (uint64_t)tv.tv_sec * 1000 + tv.tv_usec / 1000;
}

static void cli_progress(uint64_t done, uint64_t total)
{
    cli_total = total;
    if (total == 0) return;
    int pct = (int)(done * 100 / total);
    int bar_w = 30;
    int filled = bar_w * pct / 100;
    fprintf(stderr, "\r  [");
    for (int i = 0; i < bar_w; i++) fputc(i < filled ? '=' : (i == filled ? '>' : ' '), stderr);
    fprintf(stderr, "] %3d%%  ", pct);

    /* Human-readable sizes */
    const char *units[] = {"B","KB","MB","GB"};
    int ui = 0; double ds = done;   while (ds >= 1024 && ui < 3) { ds /= 1024; ui++; }
    int uj = 0; double ts = total;  while (ts >= 1024 && uj < 3) { ts /= 1024; uj++; }
    fprintf(stderr, "%.1f%s / %.1f%s", ds, units[ui], ts, units[uj]);

    /* Speed */
    uint64_t elapsed = now_ms() - cli_start_ms;
    if (elapsed > 0) {
        double speed = (double)done / ((double)elapsed / 1000.0);
        int sk = 0; while (speed >= 1024 && sk < 3) { speed /= 1024; sk++; }
        fprintf(stderr, "  %.1f%s/s", speed, units[sk]);
    }
    fflush(stderr);
}

static void cli_error(const char *msg)
{
    fprintf(stderr, "\nError: %s\n", msg);
}

static void cli_done(void)
{
    uint64_t elapsed = now_ms() - cli_start_ms;
    fprintf(stderr, "\n\nTransfer complete!\n");
    fprintf(stderr, "  Size:     ");
    const char *units[] = {"B","KB","MB","GB"};
    int u = 0; double s = cli_total; while (s >= 1024 && u < 3) { s /= 1024; u++; }
    fprintf(stderr, "%.1f %s\n", s, units[u]);
    fprintf(stderr, "  Duration: %.1fs\n", elapsed / 1000.0);
    if (elapsed > 0 && cli_total > 0) {
        double speed = (double)cli_total / ((double)elapsed / 1000.0);
        int sk = 0; while (speed >= 1024 && sk < 3) { speed /= 1024; sk++; }
        fprintf(stderr, "  Speed:    %.1f %s/s\n", speed, units[sk]);
    }
}

/* ── Help ──────────────────────────────────────────────────── */

static void print_help(const char *prog)
{
    printf("Usage: %s [OPTIONS] PATH\n\n", prog);
    printf("CLI mode — LAN file transfer tool\n\n");
    printf("Options:\n");
    printf("  -h, --help            Show this help\n");
    printf("  -v, --version         Show version\n");
    printf("  -S                    Shortcut for --mode=S (send)\n");
    printf("  -R                    Shortcut for --mode=R (receive)\n");
    printf("  --mode=S|R            Transfer mode (required)\n");
    printf("  --protocol=TCP|UDP    Protocol (default: TCP)\n");
    printf("  -p, --port=NUM        Port number (default: 9876)\n");
    printf("  --address=IP          Target IP (send: required; recv: default 0.0.0.0)\n");
    printf("  --history             Show transfer history\n\n");
    printf("Examples:\n");
    printf("  Send:   %s --mode=S --address=192.168.1.100 ./file.pdf\n", prog);
    printf("  Send:   %s -S -p 1234 ./video.mp4\n", prog);
    printf("  Recv:   %s --mode=R ./downloads/\n", prog);
    printf("  Recv:   %s --mode=R --address=10.0.0.5 -p 5555 ./received/\n", prog);
}

/* ── Main ──────────────────────────────────────────────────── */

int cli_main(int argc, char **argv)
{
    struct cli_config cfg;
    memset(&cfg, 0, sizeof(cfg));
    cfg.protocol = FT_PROTO_TCP;
    cfg.port = FT_DEFAULT_PORT;
    strncpy(cfg.address, "0.0.0.0", sizeof(cfg.address) - 1);
    cfg.mode = -1;

    /* ── Parse args ──────────────────────────────────────── */
    static struct option long_opts[] = {
        {"help",     no_argument,       0, 'h'},
        {"version",  no_argument,       0, 'v'},
        {"protocol", required_argument, 0, 1000},
        {"mode",     required_argument, 0, 1001},
        {"port",     required_argument, 0, 'p'},
        {"address",  required_argument, 0, 1002},
        {"history",  no_argument,       0, 1003},
        {0, 0, 0, 0}
    };

    int opt;
    while ((opt = getopt_long(argc, argv, "hvSRp:", long_opts, NULL)) != -1) {
        switch (opt) {
        case 'h': print_help(argv[0]); return 0;
        case 'v': printf("%s\n", CLI_VERSION); return 0;
        case 'S': cfg.mode = 0; break;
        case 'R': cfg.mode = 1; break;
        case 'p': cfg.port = atoi(optarg); break;
        case 1000: /* --protocol */
            cfg.protocol = (strcasecmp(optarg, "UDP") == 0) ? FT_PROTO_UDP : FT_PROTO_TCP;
            break;
        case 1001: /* --mode */
            cfg.mode = (strcasecmp(optarg, "R") == 0) ? 1 : 0;
            break;
        case 1002: /* --address */
            strncpy(cfg.address, optarg, sizeof(cfg.address) - 1);
            break;
        case 1003: /* --history */
            cfg.show_history = true;
            break;
        default:
            print_help(argv[0]);
            return 1;
        }
    }

    /* ── History ─────────────────────────────────────────── */
    if (cfg.show_history) {
        /* Read and print history file */
        char path[512];
        snprintf(path, sizeof(path), "./lanft_history.dat");
        FILE *fp = fopen(path, "r");
        if (!fp) { printf("No history found.\n"); return 0; }
        char line[1024];
        printf("%-20s %8s %8s %8s %4s %5s %5s %3s %s\n",
               "Name","Start","End","Dur(ms)","Kind","Port","St","%","Speed");
        while (fgets(line, sizeof(line), fp)) {
            char name[256]="",st[32]="",et[32]="",kind[8]="",status[8]="";
            unsigned long dur=0,spd=0; int k=0,port=0,stt=0,prog=0;
            sscanf(line,"%255[^|]|%31[^|]|%31[^|]|%lu|%d|%d|%d|%d|%lu",
                   name, st, et, &dur, &k, &port, &stt, &prog, &spd);
            printf("%-20s %8s %8s %8lu %4s %5d %5s %3d%% %lu\n",
                   name, st, et, dur, k==0?"SEND":"RECV", port,
                   stt==0?"OK":(stt==1?"BAD":"STOP"), prog, spd);
        }
        fclose(fp);
        return 0;
    }

    /* ── Validation ──────────────────────────────────────── */
    if (cfg.mode < 0) {
        fprintf(stderr, "Error: --mode is required (S=send, R=receive)\n\n");
        print_help(argv[0]);
        return 1;
    }

    /* Positional arg */
    if (optind >= argc) {
        fprintf(stderr, "Error: missing PATH argument\n\n");
        print_help(argv[0]);
        return 1;
    }
    strncpy(cfg.path, argv[optind], sizeof(cfg.path) - 1);

    /* Validate path */
    struct stat st;
    if (cfg.mode == 0) { /* send */
        if (stat(cfg.path, &st) != 0) {
            fprintf(stderr, "Error: file/directory not found: %s\n", cfg.path);
            return 1;
        }
    } else { /* receive */
        if (stat(cfg.path, &st) != 0 || !S_ISDIR(st.st_mode)) {
            fprintf(stderr, "Error: save directory not found: %s\n", cfg.path);
            return 1;
        }
    }

    /* ── Set CLI callbacks ────────────────────────────────── */
    transfer_set_callbacks(cli_progress, cli_error, cli_done);

    /* ── Execute ──────────────────────────────────────────── */
    struct net_context *nc = net_create(cfg.protocol);
    if (!nc) {
        fprintf(stderr, "Error: failed to create network context\n");
        return 1;
    }

    int ret = 1;
    cli_start_ms = now_ms();

    if (cfg.mode == 0) {
        /* Send — client connects to receiver */
        fprintf(stderr, "Connecting to %s:%d...\n", cfg.address, cfg.port);
        /* Try connect with retry */
        int tries = 0;
        while (tries < 60) {
            if (cfg.protocol == FT_PROTO_TCP) {
                if (net_connect(nc, cfg.address, cfg.port) == 0) break;
            } else {
                if (net_udp_bind(nc, cfg.port) == 0) {
                    net_udp_set_peer(nc, cfg.address, cfg.port);
                    break;
                }
            }
            tries++;
            fprintf(stderr, "\r  Retrying... (%d/60)", tries);
            fflush(stderr);
            sleep(1);
        }
        if (tries >= 60) {
            fprintf(stderr, "\nError: failed to connect to %s:%d\n", cfg.address, cfg.port);
            net_destroy(nc);
            return 1;
        }
        fprintf(stderr, "\nConnected! Sending %s...\n", cfg.path);
        transfer_send(nc, cfg.path, cfg.protocol);
    } else {
        /* Receive — server listens */
        fprintf(stderr, "Listening on %s:%d...\n", cfg.address, cfg.port);
        if (cfg.protocol == FT_PROTO_TCP) {
            if (net_listen_ip(nc, cfg.address, cfg.port) != 0) {
                fprintf(stderr, "Error: failed to listen on %s:%d\n", cfg.address, cfg.port);
                net_destroy(nc);
                return 1;
            }
        } else {
            if (net_udp_bind(nc, cfg.port) != 0) {
                fprintf(stderr, "Error: failed to bind UDP port %d\n", cfg.port);
                net_destroy(nc);
                return 1;
            }
        }
        fprintf(stderr, "Waiting for sender...\n");
        transfer_recv(nc, cfg.path, cfg.protocol);
    }

    net_destroy(nc);
    return ret;
}
```

- [ ] **Step 3: Commit**

```bash
cd /home/zqw/biancheng/Project/websocket && git add src/cli.c && git commit -m "feat: add CLI mode with arg parsing, progress bar, send/recv"
```

---

### Task 3: Update main.c — routing and default callbacks

**Files:**
- Modify: `src/main.c`

- [ ] **Step 1: Add cli_main declaration and SDL callback init**

At the top of main.c, after the includes, add:

```c
/* CLI entry point (cli.c) */
int cli_main(int argc, char **argv);

/* Default SDL callbacks for GUI mode */
static void gui_progress(uint64_t done, uint64_t total)
{
    struct event_progress *p = calloc(1, sizeof(*p));
    if (!p) return;
    p->bytes_done = done;
    p->bytes_total = total;
    SDL_Event event; SDL_memset(&event, 0, sizeof(event));
    event.type = USEREVENT_PROGRESS;
    event.user.data1 = p;
    SDL_PushEvent(&event);
}

static void gui_error(const char *msg)
{
    struct event_error *e = calloc(1, sizeof(*e));
    if (!e) return;
    strncpy(e->message, msg, sizeof(e->message) - 1);
    SDL_Event event; SDL_memset(&event, 0, sizeof(event));
    event.type = USEREVENT_ERROR;
    event.user.data1 = e;
    SDL_PushEvent(&event);
}

static void gui_done(void)
{
    SDL_Event event; SDL_memset(&event, 0, sizeof(event));
    event.type = USEREVENT_XFER_DONE;
    SDL_PushEvent(&event);
}
```

- [ ] **Step 2: Add CLI routing at start of main()**

Right at the beginning of `main()`, before `SDL_Init`:

```c
int main(int argc, char **argv)
{
    /* CLI mode: any args → run CLI, no SDL */
    if (argc > 1) {
        return cli_main(argc, argv);
    }

    if (SDL_Init(SDL_INIT_VIDEO) != 0) {
```

- [ ] **Step 3: Set GUI callbacks after SDL init**

After the `ui_init()` call succeeds, add:

```c
    /* Set GUI callbacks to push SDL events */
    transfer_set_callbacks(gui_progress, gui_error, gui_done);
```

- [ ] **Step 4: Build to verify**

```bash
cd /home/zqw/biancheng/Project/websocket/build && cmake .. && make -j$(nproc) 2>&1
```

Expected: Clean build. Run `./lanft --help` to verify CLI works.

- [ ] **Step 5: Commit**

```bash
cd /home/zqw/biancheng/Project/websocket && git add src/main.c && git commit -m "feat: route argc>1 to CLI mode, set SDL callbacks for GUI"
```

---

### Task 4: Update CMakeLists.txt

**Files:**
- Modify: `CMakeLists.txt`

- [ ] **Step 1: Add cli.c to SOURCES**

```cmake
set(SOURCES
    src/main.c
    src/network.c
    src/scanner.c
    src/transfer.c
    src/ui.c
    src/cli.c
)
```

- [ ] **Step 2: Build and commit**

```bash
cd /home/zqw/biancheng/Project/websocket/build && cmake .. && make -j$(nproc) 2>&1
cd /home/zqw/biancheng/Project/websocket && git add CMakeLists.txt && git commit -m "build: add cli.c to CMakeLists"
```

---

### Task 5: Build, test, and fix

- [ ] **Step 1: Full clean build**

```bash
cd /home/zqw/biancheng/Project/websocket/build && cmake .. && make -j$(nproc) 2>&1
```
Expected: Clean build.

- [ ] **Step 2: Test CLI help**

```bash
./build/lanft --help
```
Expected: Help text printed.

- [ ] **Step 3: Test CLI version**

```bash
./build/lanft --version
```
Expected: `lanft v1.0`

- [ ] **Step 4: Test CLI validation (missing mode)**

```bash
./build/lanft ./somefile 2>&1; echo "exit=$?"
```
Expected: Error message, exit code 1.

- [ ] **Step 5: Test CLI history**

```bash
./build/lanft --history
```

- [ ] **Step 6: Test GUI still works**

```bash
./build/lanft
```
Expected: SDL window opens normally.

- [ ] **Step 7: Test end-to-end CLI transfer**

Terminal 1 (receiver):
```bash
./build/lanft --mode=R ./downloads/
```
Terminal 2 (sender):
```bash
./build/lanft --mode=S --address=127.0.0.1 ./build/lanft
```
Expected: Progress bar shows, file transferred successfully.

- [ ] **Step 8: Fix any issues and commit**

```bash
cd /home/zqw/biancheng/Project/websocket && git add -A && git commit -m "fix: CLI mode end-to-end testing fixes"
```
